{
  "name": "launchpadai-mcp",
  "version": "0.1.0",
  "description": "MCP server for LaunchPadAI — validate ideas, generate PRDs, and get benchmark scores from any AI client",
  "main": "dist/index.js",
  "bin": {
    "launchpadai-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "keywords": ["mcp", "launchpadai", "ai", "startup", "validation"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}
